{{ include('client/base.html.twig') }}

{% block body %}
    <h1><center>consulte Commande</center></h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>DateCom</th>
                <td>{{ commande.dateCom ? commande.dateCom|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary btn-lg" href="{{ path('commande_index') }}">return</a>

    <a class="btn btn-success" href="{{ path('commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}


