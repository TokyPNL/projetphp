{{ include('client/base.html.twig') }}


{% block body %}
    <h1><center>consulte Categorie</center></h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>LibelleCat</th>
                <td>{{ categorie.libelleCat }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary btn-lg" href="{{ path('categorie_index') }}">return</a>

    <a class="btn btn-success" href="{{ path('categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
